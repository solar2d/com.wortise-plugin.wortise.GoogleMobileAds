Sources for the plugin `plugin.wortise.GoogleMobileAds`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.wortise.GoogleMobileAds" = {
            publisherId = "com.wortise",
        },
    },
}
```
